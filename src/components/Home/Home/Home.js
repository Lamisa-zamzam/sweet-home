import React from 'react';
import Header from '../../Shared/Header/Header';
import Slider from '../Slider/Slider';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <Slider></Slider>
        </div>
    );
};

export default Home;